<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title></title>
  <style media="screen">
    * {
      text-align: center;
    }

.row {
      display: flex;
      width: 80
    }
.column {
      flex: 30%;
      padding: 10px;
      height: 150px;
    }

    .new{
      margin-right: 200px;
      padding:20px;
    }
  </style>
</head>

<body>

<img src="<%= trip.img_url %>" alt="">
  <h1><%=trip.airline_name %></h1>
  <h3><%=trip.description %></h3>
  <h1>Trips Planned</h1>
  <div class="row">
    <div class="column">
      <h2>  <%= trip.trip_name %></h2>
      <p><%= trip.date %></p>
    </div>
    <div class="column">
      <h2>Airline:</h2>
      <a href="/airlines/<%= trip.id %>">
          <%= trip.airline_name %>
      </a>
    </div>
    <div class="column">
      <h3>From:</h3>
      <p>  <%= trip.from %></p>
      <h3>To:</h3>
      <p>  <%= trip.to %></p><br/>
      <a href="/edit/<%= trip.id %>">Edit</a>


    </div>
</body>
</html>
